<section class="section">
  <div class="container">
    <div class="columns is-centered has-text-centered">
      <h1 class="title">パスワード変更</h1>
    </div>
    </br>
    <div class="columns is-centered has-text-centered">
      <p>以下の項目にご記入の上、「変更」ボタンを押してください。</p>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-5">
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          <%= f.hidden_field :reset_password_token %>
          <div class="box">
            <div class="field">
              <%= f.label :password, "新しいパスワード", class: "label required" %>
              <p class="control has-icons-left">
                <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "input is-link" %>
                <span class="icon is-small is-left"> 
                  <i class="fas fa-lock"></i>
                </span>
              </p>
              <% if @minimum_password_length %>
                <em>※６文字以上入力してください。</em>
              <% end %>
            </div>
          
            <div class="field">
              <%= f.label :password_confirmation, "パスワード（確認）", class: "label required" %>
              <p class="control has-icons-left">
                <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input is-link" %>
                <span class="icon is-small is-left">
                 <i class="fas fa-lock"></i>
                </span>
              </p>
            </div>
          
            <div class="actions">
              <div class="has-text-centered">
                <%= f.submit "変更", class: "button is-link pl-6 pr-6" %>
              </div>
            </div>
          </div>
        <% end %>
        
        <%#= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</section>



